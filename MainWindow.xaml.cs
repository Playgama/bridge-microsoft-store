using Microsoft.UI.Xaml; using Windows.Services.Store; using WinRT.Interop; using PlaygamaBridgeMicrosoftStore.Server;  namespace PlaygamaBridgeMicrosoftStore {     public sealed partial class MainWindow : Window     {         private readonly StoreContext _store;         private readonly nint _hwnd;         private LocalAssetServer? _assetServer;          public MainWindow()         {             InitializeComponent();              _hwnd = WindowNative.GetWindowHandle(this);              _store = StoreContext.GetDefault();             InitializeWithWindow.Initialize(_store, _hwnd);              SetInitialSize(1100, 720);              _ = InitializeAsync();         }     } } 