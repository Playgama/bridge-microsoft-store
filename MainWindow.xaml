<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PlaygamaBridgeMicrosoftStore.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PlaygamaBridgeMicrosoftStore"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="PlaygamaBridgeMicrosoftStore"
    Width="1100" Height="720">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="340" />
        </Grid.ColumnDefinitions>

        <wv2:WebView2 x:Name="GameWebView" Grid.Column="0" />

        <StackPanel Grid.Column="1" Padding="12" Spacing="8">
            <TextBlock Text="Host → Web message" FontWeight="SemiBold" />
            <TextBox x:Name="MessageTextBox" PlaceholderText="Message to web…" />
            <Button x:Name="SendButton" Content="Send" Click="SendButton_Click" />

            <TextBlock Text="Log (newest first)" Margin="0,12,0,0" FontWeight="SemiBold" />
            <ListBox x:Name="LogListBox" Height="480" />
        </StackPanel>
    </Grid>
</Window>
